<UserControl x:Class="OrbitDock.App.ProjectsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="220" Focusable="False">
    <Grid Margin="8,4,8,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="2,0,2,10">
            <TextBlock Text="PROJECTS / METIS" FontSize="10" FontWeight="SemiBold" Foreground="{DynamicResource Pandora.MutedBrush}"/>
            <TextBlock x:Name="ProjectsSummary" Text="No projects registered" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,5,0,8" Foreground="{DynamicResource Pandora.TextBrush}"/>
            <WrapPanel>
                <Button x:Name="RegisterDashboardButton" Content="Add dashboard…" Click="AddDashboard_Click" Margin="0,0,6,4" Padding="9,5" ToolTip="Register one exact local HTML file. Pandora never edits its project state."/>
                <Button x:Name="RefreshProjectsButton" Content="Refresh" Click="Refresh_Click" Margin="0,0,0,4" Padding="9,5"/>
            </WrapPanel>
        </StackPanel>
        <TextBlock x:Name="ProjectsStatus" Grid.Row="1" TextWrapping="Wrap" FontSize="11" Margin="2,0,2,9" Foreground="{DynamicResource Pandora.MutedBrush}"/>
        <ScrollViewer x:Name="ProjectsScroll" Grid.Row="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" CanContentScroll="False">
            <StackPanel x:Name="ProjectsList"/>
        </ScrollViewer>
    </Grid>
</UserControl>
