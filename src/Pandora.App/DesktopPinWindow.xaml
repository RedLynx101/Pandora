<Window x:Class="Pandora.App.DesktopPinWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pandora Desktop Pin"
        Width="98"
        Height="92"
        MinWidth="72"
        MinHeight="72"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="False"
        Foreground="{DynamicResource Pandora.TextBrush}"
        Loaded="Window_Loaded"
        SourceInitialized="Window_SourceInitialized"
        Deactivated="Window_Deactivated"
        LocationChanged="Window_PlacementChanged">
    <Border x:Name="Frame"
            Background="{DynamicResource Pandora.GlassBrush}"
            BorderBrush="{DynamicResource Pandora.BorderBrush}"
            BorderThickness="1"
            CornerRadius="{DynamicResource Pandora.ItemCornerRadius}"
            Padding="{DynamicResource Pandora.ItemPadding}"
            MouseLeftButtonDown="Frame_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="16" ShadowDepth="5" Opacity="0.18" Color="#000000" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Image x:Name="PinIcon"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   SnapsToDevicePixels="True" />
            <TextBlock x:Name="PinName"
                       Grid.Row="1"
                       Foreground="{DynamicResource Pandora.TextBrush}"
                       FontFamily="Segoe UI"
                       FontSize="11"
                       LineHeight="13"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       TextTrimming="CharacterEllipsis"
                       Margin="0,5,0,0" />
        </Grid>
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Open" Click="Open_Click" />
                <MenuItem Header="Send to a Dock" Click="SendToDock_Click" />
                <MenuItem Header="Reveal in Explorer" Click="Reveal_Click" />
                <Separator />
                <MenuItem Header="Remove from Desktop" Click="Remove_Click" />
            </ContextMenu>
        </Border.ContextMenu>
    </Border>
</Window>
